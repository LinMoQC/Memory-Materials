import React from "react";
import { TestProps } from './config';

const Test: React.FC<TestProps> = (props) => {
    return (
        <div>
            Test
        </div>
    );
};

export default Test;
